/**
 * Fonctions par defaut
 */

// bouton Tout valider : A implementer
function toutValider() { 
	// A remplir avec le code
}
// bouton Tout annuler : A implementer
function toutAnnuler() { 
	// A remplir avec le code
}
// bouton Tout retablir : A implementer
function toutRetablir() { 
	// A remplir avec le code
}
// bouton Valider : A implementer
function valider() { 
	// A remplir avec le code
}
// bouton Retablir : A implementer
function retablir() { 
	// A remplir avec le code
}
// bouton Supprimer : A implementer
function supprimer() { 
	// A remplir avec le code
}
// bouton Fermer : A implementer
function fermer() { 
	// A remplir avec le code
}
// bouton Rechercher : A implementer
function rechercher() { 
	// A remplir avec le code
}
// bouton Forcer : A implementer
function forcer() { 
	// A remplir avec le code
}
// bouton Normaliser : A implementer
function normaliser() { 
	// A remplir avec le code
}
// bouton Annuler : A implementer
function annuler() { 
	// A remplir avec le code
}
// bouton Imprimer : A implementer
function imprimer() { 
	// A remplir avec le code
}
// bouton Calculer : A implementer
function calculer() { 
	// A remplir avec le code
}
// bouton Parcourir : A implementer
function parcourir() { 
	// A remplir avec le code
}

// bouton toutSupprimer : A implementer
function toutSupprimer() { 
	// A remplir avec le code
}
// fonction imprimer pre-remplie
// a adapter a chaque application
function imprimer() {
	print(); // appel a la fonction d impression par defaut
}
// fonction de deconnexion pre-remplie
// a adapter a chaque application
function deconnecter() {
	window.close(); // fermeture de la fenetre
}

